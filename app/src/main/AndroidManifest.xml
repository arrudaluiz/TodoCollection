<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.edu.utfpr.todocollection">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ActAbout"
            android:label="@string/title_activity_act_about"
            android:parentActivityName=".ActMain"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".ActReadTodo"
            android:label="@string/title_activity_act_read_todo"
            android:parentActivityName=".ActMain"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActHandleTodo"
            android:label="@string/title_activity_act_create_todo"
            android:parentActivityName=".ActMain"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActMain"
            android:label="@string/title_activity_act_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>